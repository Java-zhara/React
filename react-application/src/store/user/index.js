// import { TYPES } from "../action-types";

const INITIAL_STATE = {
  userName: "JAVA",
};

export const userReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
